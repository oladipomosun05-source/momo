services:
  # Backend API
  - type: web
    name: privacyframeworkb
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start

    envVars:
      - key: PORT
        value: 10000

      - key: MONGODB_URI
        value: mongodb+srv://oladipomosun05_db_user:lACTcaY0b0ExLl10@cluster0.aulccqs.mongodb.net/privacy_framework?retryWrites=true&w=majority

      - key: JWT_SECRET
        generateValue: true

      - key: CORS_ORIGIN
        value: https://privacyframework.onrender.com

  # Frontend
  - type: static_site
    name: privacyframework
    rootDir: finalyearprojectlatest
    buildCommand: npm install && npm run build
    publishDir: dist

    envVars:
      - key: VITE_API_URL
        value: https://privacyframeworkb.onrender.com

    routes:
      - type: rewrite
        source: /*
        destination: /index.html